package org.example.with_solid;

import java.math.BigDecimal;

class WiredDevice extends Device {

  protected WiredDevice(String name, BigDecimal value, DeviceColor colors) {
    super(name, value, colors);
  }
}
